Create-react-app application to build and pick colors from palettes.

Uses localstorage in lieu of a database integration.

Copy-to-clipboard, page transitions and react-router.

Emoji implmentation is so-so.
